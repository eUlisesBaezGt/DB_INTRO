-- 4
# Considere la siguiente consulta:

# SELECT Major, COUNT(*)
# FROM STUDENT
# GROUP BY Major

# El resultado de la expresión es:


# Los renglones de la tabla STUDENT los cuales serán lógicamente agrupados por el valor de MAJOR.
# La función COUNT sumará el número de renglones en cada grupo.

-- 6
# Considere la siguiente consulta:

# SELECT Major, COUNT(*)
# FROM STUDENT
# GROUP BY Major

# Las columnas resultantes en la tabla son:
# Major y COUNT(*) (el número de filas en la tabla STUDENT que tienen el mismo valor de Major)


-- 9
# En el formato SELECT-FROM-WHERE, estructura fundamental de SQL:
# La expresión SELECT especifica el nombre de todas las columnas de la tabla, FROM especifica la tabla a ser usada, y WHERE provee la condición(s) para la selección.